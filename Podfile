# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MemoAppWithFirebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MemoAppWithFirebase
  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'PKHUD', '~> 5.0'

  target 'MemoAppWithFirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MemoAppWithFirebaseUITests' do
    # Pods for testing
  end

end
